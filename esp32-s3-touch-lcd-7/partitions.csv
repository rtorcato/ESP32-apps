# One app resident at a time: the launcher stays in factory, whichever app is
# loaded lives in ota_0. See apps/launcher/README.md for why.
#
# PlatformIO uploads to ota_0 when the table has one (platform builder/main.py,
# "if partition[subtype] == ota_0: app_offset = next_offset"), so
# `pio run -e ticker -t upload` lands in the app slot with no configuration.
# The launcher is the exception and overrides it back -- scripts/launcher_offset.py.
#
#   factory   2MB      the launcher (1.3MB today)
#   ota_0     3MB      the loaded app (ticker, the largest, is 1.33MB)
#   littlefs  10.9MB   logos, thumbnails, config
#
# A trailing "# note" on a row is NOT a comment -- the parser reads it as the
# Flags column and fails the build. Keep notes up here.
#
# Name,     Type, SubType,  Offset,   Size
nvs,        data, nvs,      0x9000,   0x5000
otadata,    data, ota,      0xe000,   0x2000
factory,    app,  factory,  0x10000,  0x200000
ota_0,      app,  ota_0,    0x210000, 0x300000
littlefs,   data, spiffs,   0x510000, 0xAE0000
coredump,   data, coredump, 0xFF0000, 0x10000
